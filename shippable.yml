# Language setting
language: node_js

# Version number
node_js:
  - "0.12"


env:
  global:
    - FOO=FUBU

build:
 
  ci:
    - ps -eax
    - ls -al
   # - sleep 6m

  post_ci:
    - docker build -t=shiphitchcock2test/img-test:$BRANCH.$BUILD_NUMBER .
    - docker push shiphitchcock2test/img-test:$BRANCH.$BUILD_NUMBER

integrations:
  hub:
    - integrationName: ship-docker
      type: docker
      
